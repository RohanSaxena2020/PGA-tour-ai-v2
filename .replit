modules = ["web", "python-3.12", "nodejs-20"]
run = "npm run build && pip install -r requirements.txt"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "python server/server.py"]
build = ["sh", "-c", "npm run build && pip install -r requirements.txt"]
